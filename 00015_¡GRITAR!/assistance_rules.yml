---
rules:
  - !ruby 
    when: !ruby
      these_tests_failed:
        - gritar tiene que usar el parámetro
    then: hmm... `gritar` está devolviendo siempre lo mismo :thinking_face:; ¿estás usando el parámetro?
  - !ruby 
    when: !ruby
      these_expectations_failed:
        - "* Not:Declares:convertirEnMayuscula"
    then: no tienes que declarar `convertirEnMayuscula`; ya está lista para usar
  - !ruby 
    when: !ruby
      these_tests_failed:
        - gritar tiene que usar los signos de exclamación
    then: acuérdate de concatenar `"¡"`, el parámetro y `"!"` usando el operador `+`
